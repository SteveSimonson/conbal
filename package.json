{
  "name": "conbal",
  "private": true,
  "type": "module",
  "scripts": { "validate": "node scripts/validate-config.mjs", "test": "node --test tests/*.test.mjs", "dev": "wrangler dev --port 8895", "deploy": "wrangler deploy", "preview": "wrangler dev --remote --port 8895" },
  "devDependencies": { "wrangler": "^4.20.0" }
}
